let sum = 0;
for (let i = 0; i < 4000000; i++) {
  if ((Number.isInteger(Math.sqrt(5*(i*i)+4)) || Number.isInteger(Math.sqrt(5*(i*i)-4))) && i % 2 == 0) {
    sum = sum + i;
  }
}
console.log(sum);
// Answer is 4613732.
